#include "Engine/Thread.hpp"

//Thread::Thread(int p_id, std::vector<Thread*> p_contextualThreads, std::vector<std::mutex*> p_mutex, std::condition_variable* p_conditionVariable){
  ///  id = p_id;
   // contextualThreads = p_contextualThreads;
   // _mutex = p_mutex;
   // _conditionVariable = p_conditionVariable;
//}


